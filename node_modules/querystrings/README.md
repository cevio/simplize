## API ##

``` javascript
var querystring = require('querystring');
var querys = querystring.format(window.location.search.replace(/^\?/, ''));
// {...}

var obj = {a:1,b:2}
var string = querystring.toString(obj);
//a=1&b=2
```