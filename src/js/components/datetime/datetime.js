let currentYear = new Date().getFullYear();

let default_config = {
    format: 'YYYY-MM-DD',
    minYear: currentYear - 15,
    maxYear: currentYear + 15
};

class Datetime {
    constructor(vm) {
        
    }
}